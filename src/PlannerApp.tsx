export default function PlannerApp() {
  return (
    <div style={{ padding: "2rem", fontFamily: "sans-serif" }}>
      <h1>✅ Planner App deployed!</h1>
      <p>You can now add the full drag-and-drop interface here.</p>
    </div>
  );
}
